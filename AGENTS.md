# Agent notes

This repo uses **Ponytail** for Cursor (lazy senior / YAGNI coding mode).

- Hooks: `.cursor/hooks.json` (project-scoped; see `ponytail/docs/cursor-hooks.md`)
- Skills: `.cursor/skills/ponytail*`
- Source checkout: `ponytail/`

Levels (send as a plain chat message): `/ponytail`, `/ponytail lite|full|ultra|off`.

Do not add `.cursor/rules/ponytail.mdc` while hooks are installed — the rule and hooks are mutually exclusive.
